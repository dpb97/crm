# pilanda_sales

Frappe-App für den **Vertrieb / Sales**-Bereich im LCS-Pilanda-Stack — LCS-spezifische
Vertriebslogik rund um die Frappe-CRM-SPA (`/crm`). Teil der geteilten Pilanda-Bench
(Site `lcs.local`, mehrere Apps) — nicht standalone.

> **Stand 13.06.2026:** Repo frisch initialisiert (Git + Stack-Konventionen).
> Der eigentliche Frappe-App-Code wird mit `bench new-app pilanda_sales` erzeugt
> (kanonisches Skeleton) und dann hier eingecheckt. Bis dahin enthält das Repo
> nur die Grundkonfiguration + Konventions-Verweise.

Siehe `CLAUDE.md` für Zweck, Namespace und Stack-Konventionen.
