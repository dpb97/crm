{
  "name": "pilanda-sales-frontend",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Vue 3 Frontend von pilanda_sales. Baut das IIFE-Bundle des Vertrieb-Modul-Dashboards, das die Desk-Page sales-dashboard laedt. Theme-first: ausschliesslich --pp-*-Tokens, Bausteine per Copy-Modell aus pilanda_theme.",
  "scripts": {
    "build": "vite build --config vite.dashboard.config.js",
    "build:dashboard": "vite build --config vite.dashboard.config.js",
    "dev": "vite build --config vite.dashboard.config.js --watch"
  },
  "dependencies": {
    "vue": "^3.5.0",
    "frappe-ui": "^0.1.278"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.0",
    "vite": "^7.0.0"
  }
}
