[pre_model_sync]
# Drop legacy Custom Fields BEFORE the schema sync — their fieldnames
# now exist as native columns on CRM Lead / CRM Deal.
lcs_integrations.patches.drop_lead_deal_qualification_custom_fields
lcs_integrations.patches.v1_0.install_custom_fields
lcs_integrations.patches.seed_lookup_data
lcs_integrations.patches.migrate_legacy_source_reason
lcs_integrations.patches.remove_abas_legacy
lcs_integrations.patches.reinstall_bsm_fields
lcs_integrations.patches.fusion_upgrade_oauth
lcs_integrations.patches.install_sales_manager_fields
lcs_integrations.patches.reinstall_erpnext_project_field
lcs_integrations.patches.migrate_lead_score_to_crm_lead
lcs_integrations.patches.install_outlook_teams_fields
lcs_integrations.patches.install_followup_fields
lcs_integrations.patches.install_domain_binding_fields
lcs_integrations.patches.install_contact_release_fields

[post_model_sync]
# Needs the LCS Segment / LCS Sales Territory DocTypes synced first.
lcs_integrations.patches.seed_market_split
# Resolves sales-manager codes (JFA/PKO/CLU/DRO) to User accounts on territories.
lcs_integrations.patches.map_sales_manager_users
# Touches LCS Project rows after the new phase options exist.
lcs_integrations.patches.migrate_project_phase
# Adds the 'Important' star flag to CRM Lead / CRM Deal.
lcs_integrations.patches.install_important_flag
# Adds 'Expected Start Date' to CRM Lead / CRM Deal.
lcs_integrations.patches.install_estimated_start_date
# Adds 'Previous Companies' to Contact (network graph / career history).
lcs_integrations.patches.install_previous_companies
# Seeds the editable funnel phase definitions (needs LCS Funnel Phase synced).
lcs_integrations.patches.seed_funnel_phases
# Exposes CRM Deal `currency` in the create dialog + side panel layouts.
lcs_integrations.patches.install_deal_currency_field
# Relabels CRM Deal `annual_revenue` -> "Geschätzter Umsatz" (deal value, not company revenue).
lcs_integrations.patches.relabel_deal_annual_revenue
# Backfills LCS Project.currency from the linked deal (default EUR).
lcs_integrations.patches.backfill_project_currency
# Adds company_name (Firma) as a quick filter on the Contacts list.
lcs_integrations.patches.add_contact_company_quick_filter
# Seeds a small demo agenda for the weekly sales meeting (idempotent, skips if any exist).
lcs_integrations.patches.seed_sales_meeting_agenda
# Seeds the 3 demo Chancen from the klickdummy (idempotent, skips if any exist).
lcs_integrations.patches.seed_lcs_chances
# Pins the Pilanda brand colour to LCS teal (theme settings shipped stale navy).
lcs_integrations.patches.set_pilanda_brand_teal
# Extends the Projekt funnel group with Ausführung + Abgeschlossen.
lcs_integrations.patches.extend_funnel_execution_completed
# Demo-data cleanup: logical project numbers/names + type for the deal-projects.
lcs_integrations.patches.rename_demo_projects
# Backfill: bind existing contacts to their customer by mail domain (+company_name).
lcs_integrations.patches.backfill_contact_domain_binding
# Cross-org / person relationships (Partner/Zulieferer, Arbeitgeber-Historie, Freunde).
lcs_integrations.patches.add_cross_org_relations
# Roles that authorise offer approvals (CEO / CFO-COO / owner).
lcs_integrations.patches.create_offer_approver_roles
# Pin the site timezone to Europe/Vienna (empty default = Asia/Kolkata, +3:30 skew).
lcs_integrations.patches.set_system_timezone_vienna
# Sales-Meeting classification fields on LCS Project (offer pipeline from the Excel protocol).
lcs_integrations.patches.install_sales_meeting_fields
# Store Graph conversationId on synced mail for exact conversation threading.
lcs_integrations.patches.install_conversation_id
# Turn Contact.previous_companies free text into a CRM Organization picker.
lcs_integrations.patches.convert_previous_company_to_link
# abas project description (Sucherweiterung) as its own field on LCS Project.
lcs_integrations.patches.install_abas_desc
# Opportunity-Matrix (5 dimensions) on LCS Project for the inspector radar.
lcs_integrations.patches.install_project_matrix
# Contact honorific title field (lcs_title) + seed the usual salutations (Anrede).
lcs_integrations.patches.install_contact_title_and_salutations
# Default currency EUR (empty default fell back to INR → ₹) + relabel stray INR.
lcs_integrations.patches.set_default_currency_eur
# E-mail visibility flags (lcs_shared/lcs_internal) on Communication + backfill.
lcs_integrations.patches.install_email_visibility_flags
# Delete existing internal->internal noise mail (not shared) — user cleanup.
lcs_integrations.patches.purge_internal_internal_mail
